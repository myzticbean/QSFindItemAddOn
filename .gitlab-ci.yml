image: maven:3.8.3-openjdk-17

build:
  stage: Build
  script:
    - echo "========== Build Started =========="
    - mvn clean package -DskipTests
  allow_failure: false
  artifacts:
    when: on_success
    paths:
      - target/*.jar